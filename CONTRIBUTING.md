# Guide de contribution

Merci de ton intérêt pour contribuer au Planificateur de révision KOPIO ! 🎉

## Comment contribuer

### Signaler un bug

1. Vérifie que le bug n'a pas déjà été signalé dans les [Issues](https://github.com/kopio/planificateur-revision-kopio/issues)
2. Si ce n'est pas le cas, crée une nouvelle issue avec :
   - Un titre clair et descriptif
   - Une description détaillée du problème
   - Les étapes pour reproduire le bug
   - Le comportement attendu vs le comportement actuel

### Proposer une fonctionnalité

1. Vérifie que la fonctionnalité n'a pas déjà été proposée
2. Crée une issue avec le label "enhancement"
3. Décris la fonctionnalité et explique pourquoi elle serait utile

### Soumettre du code

1. Fork le repository
2. Crée une branche depuis `main` : `git checkout -b feature/ma-fonctionnalite`
3. Fais tes modifications
4. Assure-toi que le code respecte les standards (lint, format)
5. Commit avec des messages clairs : `git commit -m "feat: ajoute la fonctionnalité X"`
6. Push vers ta branche : `git push origin feature/ma-fonctionnalite`
7. Ouvre une Pull Request

## Standards de code

- Utilise TypeScript pour tout le code
- Suis les conventions de nommage React/TypeScript
- Ajoute des commentaires pour le code complexe
- Écris des tests si possible

## Convention de commit

Utilise des messages de commit clairs et descriptifs :
- `feat:` pour une nouvelle fonctionnalité
- `fix:` pour une correction de bug
- `docs:` pour la documentation
- `style:` pour le formatage
- `refactor:` pour le refactoring
- `test:` pour les tests

## Questions ?

N'hésite pas à ouvrir une discussion sur GitHub ou à nous contacter à contact@kopio.eu

